# StoreSwitcher Vault package feed

Add this repository URL to Sileo or Zebra:

`https://raw.githubusercontent.com/hnduy910/StoreSwitcherVault-repo/main/`

This public repository contains only package-feed metadata and the compiled `.deb`. The source project is maintained separately from this package feed.

The package targets Dopamine/rootless iOS 15–16 and depends on ElleKit (or MobileSubstrate compatibility).
